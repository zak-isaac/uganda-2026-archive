Upload firebase-config.js to your GitHub repository root.

In Firebase Console:
1. Build > Authentication > Sign-in method > Enable Email/Password.
2. Build > Firestore Database > Rules.
3. Replace the rules with firestore.rules contents.
4. Publish.

Your index.html must include firebase-config.js before the Firebase cloud sync module.
